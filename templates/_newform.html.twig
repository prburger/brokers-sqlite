<div class="form_div">
    {{ form_start(form) }}

    <div class="row">
            <div class="col-sm-6">
                {{ form_widget(form) }}
                {{ form_widget(contact) }}
            </div>
            <div class="col-sm-6 form_button">
                <button class="btn btn-primary pull-right">{{ button_label|default('Save') }}</button>
            </div>
    </div>
    {{ form_end(form) }}
</div>