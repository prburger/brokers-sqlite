{% extends 'base.html.twig' %}

{% block title %}{{'broker_index'|trans|raw}}{% endblock %}

{% block body %}
    
<div class="row">
    <div class="page_form_heading">{{'broker_index'|trans|raw}}
        <div class="return_to_list_label">
            <a href="{{ path('broker_new') }}" title="{{'create_new_broker'|trans|raw}}"><i class="fas fa-plus page_icon"></i></i></a>
            <a href="{{ path('portal_index') }}" title="{{'back_to_portal'|trans|raw}}"><i class="fas fa-undo page_icon"></i></a>    
        </div>
    </div>
</div>

<div class="row">
     <table class="table">
        <thead>
            <tr>
                <th>{{'table.column.name'|trans|raw}}</th>
                <th>{{'table.column.date.added'|trans|raw}}</th>
                <th>{{'table.column.date.edited'|trans|raw}}</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
          
        {% for broker in paginator.results %}
            <tr>
                <td>{{ broker.name }}</td>
                <td>{{ broker.dateAdded ? broker.dateAdded|date('Y-m-d') : '' }}</td>
                <td>{{ broker.dateEdited ? broker.dateEdited|date('Y-m-d') : '' }}</td>
                <td class="action_column">
                    <a href="{{ path('broker_show', {'id': broker.id }) }}" title="{{'action_show'|trans|raw}}"><i class="fas fa-glasses action_icon"></i></a>
                    <a href="{{ path('broker_edit', {'id': broker.id }) }}" title="{{'action_edit'|trans|raw}}"><i class="far fa-edit action_icon"></i></a>
                </td>
            </tr>
        {% else %}
                {%include 'no_records_found.php' %}
        {% endfor %}
   </tbody>
</table>
    
 {% if paginator %} 
         <div class="navigation text-center">
            <ul class="pagination">
                {% if paginator.hasPreviousPage %}
                    <li class="prev"><a href="{{ path('broker_index_paginated', {page: paginator.previousPage}) }}" rel="previous"><i class="fa fw fa-long-arrow-left"></i> {{ 'paginator.previous'|trans }}</a></li>
                {% else %}
                    <li class="prev disabled"><span><i class="fa fw fa-arrow-left"></i> {{ 'paginator.previous'|trans }}</span></li>
                {% endif %}

                {% for i in 1..paginator.lastPage %}
                    {% if i == paginator.currentPage %}
                        <li class="active"><span>{{ i }} <span class="sr-only">{{ 'paginator.current'|trans }}</span></span></li>
                    {% else %}
                        <li><a href="{{ path('broker_index_paginated', {page: i}) }}">{{ i }}</a></li>
                    {% endif %}
                {% endfor %}

                {% if paginator.hasNextPage %}
                    <li class="next"><a href="{{ path('broker_index_paginated', {page: paginator.nextPage}) }}" rel="next">{{ 'paginator.next'|trans }} <i class="fa fw fa-arrow-right"></i></a></li>
                {% else %}
                    <li class="next disabled"><span>{{ 'paginator.next'|trans }} <i class="fa fw fa-arrow-right"></i></span></li>
                {% endif %}
            </ul>
        </div>
     {% endif %} 
   
</div> 
{#
 {% for post in paginator.results %}
        <article class="post">
            <h2>
                <a href="{{ path('broker_index') }}">
                    {{ post.name }}
                </a>
            </h2>

            <p class="post-metadata">
                <span class="metadata"><i class="fa fa-calendar"></i> {{ post.dateAdded|format_datetime('short', 'medium', '', 'UTC') }}</span>
                <span class="metadata"><i class="fa fa-user"></i> {{ post.dateEdited |format_datetime('short', 'medium', '', 'UTC') }}</span>
            </p>

             <p>{{ post.summary }}</p> 

             {{ include('blog/_post_tags.html.twig') }} 
        </article>
    {% else %}
        <div class="well">{{ 'post.no_posts_found'|trans }}</div>
    {% endfor %}
    {% if paginator.hasToPaginate %}
        <div class="navigation text-center">
            <ul class="pagination">
                {% if paginator.hasPreviousPage %}
                    <li class="prev"><a href="{{ path('broker_index_paginated', {page: paginator.previousPage, tag: tagName}) }}" rel="previous"><i class="fa fw fa-long-arrow-left"></i> {{ 'paginator.previous'|trans }}</a></li>
                {% else %}
                    <li class="prev disabled"><span><i class="fa fw fa-arrow-left"></i> {{ 'paginator.previous'|trans }}</span></li>
                {% endif %}

                {% for i in 1..paginator.lastPage %}
                    {% if i == paginator.currentPage %}
                        <li class="active"><span>{{ i }} <span class="sr-only">{{ 'paginator.current'|trans }}</span></span></li>
                    {% else %}
                        <li><a href="{{ path('broker_index_paginated', {page: i, tag: tagName}) }}">{{ i }}</a></li>
                    {% endif %}
                {% endfor %}

                {% if paginator.hasNextPage %}
                    <li class="next"><a href="{{ path('broker_index_paginated', {page: paginator.nextPage, tag: tagName}) }}" rel="next">{{ 'paginator.next'|trans }} <i class="fa fw fa-arrow-right"></i></a></li>
                {% else %}
                    <li class="next disabled"><span>{{ 'paginator.next'|trans }} <i class="fa fw fa-arrow-right"></i></span></li>
                {% endif %}
            </ul>
        </div>
    {% endif %}
    #}
{% endblock %}
