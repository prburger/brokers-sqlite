{% extends 'base.html.twig' %}

{% block title %}{{'create_new_product'|trans|raw}}{% endblock %}

{% block body %}

    <div class="page_form_heading">{{'create_new_product'|trans|raw}}
    <a class="return_to_list_label" href="{{ path('product_index') }}" title="{{'back_to_list'|trans|raw}}">
    <i class="fas fa-undo page_icon"></i></a>
    </div>
    {{ include('product/_form.html.twig') }}
{% endblock %}
