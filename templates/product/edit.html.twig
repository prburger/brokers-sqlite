{% extends 'base.html.twig' %}

{% block title %}{{'product_edit'|trans|raw}}{% endblock %}

{% block body %}
{# <div class="form_div"> #}
<div class="page_form_heading">{{'product_edit'|trans|raw}}
    <div class="return_to_list_label">
    {{ include('product/_delete_form.html.twig') }}
    <a href="{{ path('product_index') }}" title="{{'back_to_list'|trans|raw}}"><i class="fas fa-undo page_icon"></i></a>    
    </div>
  </div>
    {{ include('product/_form.html.twig', {'button_label': 'Update'}) }}
{# </div> #}
{% endblock %}
